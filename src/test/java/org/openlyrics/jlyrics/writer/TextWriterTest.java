package org.openlyrics.jlyrics.writer;
/*
import org.junit.jupiter.api.Test;
import org.openlyrics.jlyrics.IOFactory;
import org.openlyrics.jlyrics.Song;
import org.openlyrics.jlyrics.reader.ReaderType;
import org.openlyrics.jlyrics.transform.SongTransformer;
import org.openlyrics.jlyrics.transform.SongTransformerConfig;

import java.io.FileInputStream;
import java.io.FileOutputStream;
*/
class TextWriterTest {
/*
    @Test
    void testTextFormat() throws Exception {
        Song song = IOFactory.getNewReader(ReaderType.OPENLYRICS).read(new FileInputStream("001.xml"));
        SongTransformerConfig config = new SongTransformerConfig();
        config.setIntroSongBook(true).setIntroSongNumber(true).setSongbookNameShortener(name-> "H");
        song = new SongTransformer().transform(song, config);
//        IOFactory.createWriter(WriterType.TEXT).save(song, new FileOutputStream("001.txt"));
        IOFactory.getNewWriter(WriterType.RTF).write(song, new FileOutputStream("001.rtf"));
    }
 */
}
