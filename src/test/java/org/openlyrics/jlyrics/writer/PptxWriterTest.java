package org.openlyrics.jlyrics.writer;
/*
import org.junit.jupiter.api.Test;
import org.openlyrics.jlyrics.IOFactory;
import org.openlyrics.jlyrics.Song;

import java.io.FileInputStream;
import java.io.FileOutputStream;

import static org.openlyrics.jlyrics.reader.ReaderType.OPENLYRICS;
import static org.openlyrics.jlyrics.writer.WriterType.PPTX;
*/
class PptxWriterTest {

/*
    @Test
    void testSavePptx() throws Exception {
        Song song = IOFactory.getNewReader(OPENLYRICS).read(new FileInputStream("target/test-classes/songs/Amazing Grace.xml"));
        song.getProperties().getTitles().get(0).setTitle("A menny az én oltalmam");
        FileOutputStream outputStream = new FileOutputStream("result.pptx");
        IOFactory.getNewWriter(PPTX).write(song, outputStream);
        outputStream.close();
    }
 */
}
